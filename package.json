{
  "name": "quantify-ai",
  "version": "1.0.0",
  "description": "Quantify Suite is a unified PWA business intelligence platform combining KPI tracking and accounting.",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:all": "cd frontend && npm run build && cd ../backend && npm run build",
    "test:backend": "cd backend && npm run test",
    "test:e2e": "npx playwright test",
    "test:all": "npm run test:backend && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}